<Window x:Class="resumeadaptorWPF.Views.deleteSubSectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:resumeadaptorWPF.Views"
        mc:Ignorable="d"
        Title="deleteSubSectionView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="9*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ComboBox Name="SubSectionCombo" SelectionChanged="SubSectionCombo_SelectionChanged"></ComboBox>
        <Button Grid.Column="1" Name="DeleteSubSectionButton" Click="DeleteSubSectionButton_Click"  Command="{Binding Path = DeleteCommand }" >Delete Sub Section</Button>

    </Grid>
</Window>
